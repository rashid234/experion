// for (let i = 0; i < 5; i++){  
    // if we use let the variables scope is in only that block
    // var is declared as global variable
for (var i = 0; i < 5; i++){
    console.log('in loop', i);
}
console.log(i);

const firstName = 'Aravi'; // if we use unchanged variables should use const keyword 

const data = null;// make the value of variable is null
const data_base = 'js';
console.log(data_base);

